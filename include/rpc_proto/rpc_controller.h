#pragma once

namespace netco {
    /**
     * @brief RpcController is used to control the RPC call.
     * It provides methods to cancel the call, set the timeout, retry when the call fails, and get the status of the call.
     * 
     */
    class RpcController {

    }
}